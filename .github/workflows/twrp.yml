name: TWRP Recovery Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sistemi Hazırla
        run: |
          sudo apt update
          sudo apt install -y bc build-essential zip curl libstdc++6 git wget python3

      - name: Dosyaları Çek
        uses: actions/checkout@v4
        with:
          path: device/xiaomi/dandelion

      - name: Kaynak Kodunu İndir (Repo Sync)
        run: |
          mkdir workspace
          cd workspace
          git config --global user.name "EFE521"
          git config --global user.email "efe@gmail.com"
          # Kimlik doğrulama hatasını aşmak için git ayarı
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          
          curl https://storage.googleapis.com/git-repo-downloads/repo > repo
          chmod a+x repo
          sudo mv repo /usr/local/bin/
          
          # Şifre istemeyen minimal init
          repo init --depth=1 -u https://github.com/minimal-twrp/platform_manifest_twrp_omni.git -b twrp-11
          
          # Senkronizasyon sırasında şifre sormasını tamamen engellemek için
          repo sync -j$(nproc --all) --force-sync --no-clone-bundle --no-tags --optimized-fetch --prune

      - name: Derlemeyi Başlat
        run: |
          cd workspace
          cp -r ../device/xiaomi/dandelion device/xiaomi/dandelion
          . build/envsetup.sh
          export ALLOW_MISSING_DEPENDENCIES=true
          lunch omni_dandelion-eng
          mka recoveryimage -j$(nproc --all)

      - name: Dosyayı Kaydet (Upload)
        uses: actions/upload-artifact@v4
        with:
          name: TWRP-Dandelion
          path: workspace/out/target/product/dandelion/recovery.img
