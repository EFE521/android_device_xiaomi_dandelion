name: TWRP Recovery Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sistemi Hazırla
        run: |
          sudo apt update
          sudo apt install -y bc build-essential zip curl libstdc++6 git wget python3
          
          # Repo aracını kur
          curl https://storage.googleapis.com/git-repo-downloads/repo > repo
          chmod a+x repo
          sudo mv repo /usr/local/bin/
          sudo ln -sf /usr/bin/python3 /usr/bin/python

      - name: Cihaz Ağacını Çek
        uses: actions/checkout@v4
        with:
          path: device/xiaomi/dandelion
          persist-credentials: false # KRİTİK: GitHub'ın Git bilgilerini kaydetmesini engeller

      - name: Kaynak Kodunu İndir
        run: |
          mkdir -p ~/twrp_workspace
          cd ~/twrp_workspace
          
          # Git ayarlarını sıfırla ve yapılandır
          git config --global user.name "EFE521"
          git config --global user.email "efe@gmail.com"
          
          # GitHub'ın eklediği ve hataya sebep olan header'ı hem global hem local düzeyde temizle
          git config --global --unset-all http.https://github.com/.extraheader || true
          
          # Repo başlatma ve senkronizasyon
          # --no-clone-bundle ve --no-tags kullanımı bağlantı hatalarını minimize eder
          repo init -u https://github.com/minimal-twrp-omni/platform_manifest_twrp_omni.git -b twrp-11 --depth=1 --no-clone-bundle
          
          # Senkronizasyonu başlat (Hata durumunda zorlamak için force-sync)
          repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags

      - name: Derlemeyi Başlat
        run: |
          cd ~/twrp_workspace
          # Checkout ile inen cihaz ağacını çalışma alanına taşı
          mkdir -p device/xiaomi/dandelion
          cp -r $GITHUB_WORKSPACE/device/xiaomi/dandelion/* device/xiaomi/dandelion/
          
          # Derleme komutları
          . build/envsetup.sh
          export ALLOW_MISSING_DEPENDENCIES=true
          lunch omni_dandelion-eng
          mka recoveryimage -j$(nproc --all)

      - name: Dosyayı Artifact Olarak Yükle
        uses: actions/upload-artifact@v4
        with:
          name: TWRP-Dandelion-Img
          path: ~/twrp_workspace/out/target/product/dandelion/recovery.img
